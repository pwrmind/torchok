current_metrics:
  avg_cpc: 150.0          # Стоимость клика
  base_cr: 0.02           # Базовая конверсия (при базовой цене)
  base_price: 4200.0      # Точка отсчета для эластичности
  fixed_costs: 80000.0    # Ваши личные расходы + налоги + софт
  ad_budget: 100000.0      # Рекламный бюджет в месяц
  session_duration: 1.0   # Длительность сессии в часах
  max_hours: 120          # Желаемый лимит рабочих часов в месяц

optimization_params:
  min_price: 2500.0       # Минимальный порог (демпинг)
  max_price: 15000.0      # Верхний порог (премиум)
  steps: 100000           # Количество сценариев для обсчета на GPU
  demand_elasticity: 1.8  # Чувствительность рынка (выше 1.0 — реалистично)
  avg_sessions_per_client: 4.5       # Среднее удержание (LTV фактор)
  opportunity_cost_per_hour: 1500.0  # Цена «потерянного» часа простоя
